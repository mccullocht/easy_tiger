[package]
name = "wt_mdb"
version = "0.1.0"
edition = "2024"
description = "WiredTiger bindings intended to be used MongoDB way."

[dependencies]
rustix = "1.0.7"
smallvec = "1"
tracing = "0.1.41"
wt_sys = { path = "../wt_sys" }

[dev-dependencies]
tempfile = "3.13.0"
